type ConsultaHistorico {
  id: ID!
  pacienteId: ID!
  medicoId: ID!
  dataHora: String!
  observacoes: String
}

type Query {
  listarHistorico: [ConsultaHistorico]
  listarHistoricoPorPaciente(pacienteId: ID!): [ConsultaHistorico]
}
